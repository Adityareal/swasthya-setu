<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <!-- Required. The WebView loads the bundle from disk, but every AI call goes
         to the hosted origin in NEXT_PUBLIC_API_BASE_URL. Without this the app
         still runs and every flow still completes — triage falls back to the
         local keyword classifier and the summary to a local template. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required by the QR_Module scanner at /asha/scan (Req 18.2). The WebView
         loads from https://localhost, which is a secure context, so getUserMedia
         is available there — but the WebView cannot grant itself a permission the
         app never declared, and without this line the scanner fails on every
         device with the same "camera unavailable" reading as a phone with no
         camera at all. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- `required="false"` is the load-bearing attribute. QR scanning is a
         Stretch_Capability: Req 18.5 keeps patient selection completable through
         the Patient_Picker and the manual card-number field alone, so a device
         with no camera must still install and still complete every flow. Omitting
         this attribute defaults it to true and would let the Play Store filter
         the app off exactly those devices. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
</manifest>
